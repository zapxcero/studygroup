<div class="container">
  <div class="section">
    <div class="columns">
      <div class="column has-text-centered">
        <h1 class="title" >Available Study Groups</h1>
        <br>
      </div>
    </div>
    <div class="columns is-variable is-8 is-multiline">
        <% @events.each do |event|%>
          <%= render "event", event: event, profiles: @profiles%>
        <% end %>
    </div>
  </div>
</div>